const firstName = "Denis";
const job = "Developer";
const currentYear = 2023;
const birthYear = 1993;

const msg = `I'm ${firstName}, a ${currentYear - birthYear} year old ${job}.`;

console.log(msg);
console.log(`Just a regular string is also acceptable.`);

// Creating multiline strings
console.log(`Strings with multiple lines.
Template literals are amazing.
It makes it possible.
`);